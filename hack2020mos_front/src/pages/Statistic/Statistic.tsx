import React from 'react'
import { ContentTitle, PageContent } from 'core/components'

/** Страница статистики */
export const Statistic: React.FC = React.memo(() => {
    return (
        <PageContent>
            <ContentTitle title="Здесь будут крутые графики..." />
        </PageContent>
    )
})